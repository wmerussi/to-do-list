import { Routes } from '@angular/router'

import { ResetPasswordComponent } from './reset-password.component'

export const resetPasswordRoutes: Routes = [
  {
    /** Path to resetPassword page */
    path: 'reset-password',
    component: ResetPasswordComponent,
  },
]
